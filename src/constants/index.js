const USER_REGISTER_LOGIN_MODELS = {
  EMAIL: "email",
  PHONE: "phone",
};

const MONGODB_DB = "mydatabase";
const MONGODB_URL = `mongodb://127.0.0.1:27017/${MONGODB_DB}`;

module.exports = { MONGODB_URL, USER_REGISTER_LOGIN_MODELS };
